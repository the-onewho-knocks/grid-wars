# ── Grid War Environment Config ────────────────────────────────────────────────
#
# Just set ONE variable — everything else is derived automatically.
#
# VITE_BACKEND_URL  →  used for REST API calls
#                      also auto-converted to wss:// for WebSocket
#
# Local development:
VITE_BACKEND_URL=http://localhost:8080

# Production (Railway):
# VITE_BACKEND_URL=https://grid-wars-production.up.railway.app

# ── Optional overrides (only if you need separate WS host) ────────────────────
# VITE_WS_URL=wss://grid-wars-production.up.railway.app
